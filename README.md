# GCodePreviewer

GCodePreviewer is a web application that allows users to preview 3D models generated from GCode files. 

It uses Three.js for rendering and React for the user interface.

![screenshot](screenshot.gif)